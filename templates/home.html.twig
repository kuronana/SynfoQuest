{% extends 'base.html.twig' %}

{% block body %}

    <h1>Title of the articles </h1>
    {% for article in articles %}
    <div> {{ article.title }} <a href="{{ path('blog_show', {'id': article.id }) }}">{{article.title}}</a> </div>
    {% endfor %}

{% endblock %}